function city(name,population,treasury){
const result = {
    name: name,
    population: population,
    treasury: treasury
};

return result;
}

console.log(city('Tortuga', 7000, 15000))

// tui kato promenlivata & stoinostta imat edno i sushto ime, mojem da vkarame samo promenlivata v obekta
// sukraten zapis

const result = {
    name,
    population,
    treasury
};