function concatenate(firstName, lastName, delimiter) {
    console.log(firstName + lastName + delimiter);
    console.log(`${firstName}${lastName}${delimiter}`)
}
concatenate('John', 'Smith', '->');